import styled from 'styled-components';
import CommentsBG from './assets/CommentsBG.jpeg';

export const AppContainer = styled.div`
  background: url(${CommentsBG});
  background-size: cover;
  min-height: 100vh;
`;
